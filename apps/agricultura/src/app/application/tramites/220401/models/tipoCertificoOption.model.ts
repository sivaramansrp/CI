
/**
 * Interfaz que representa una opción para Tipo de Certifico.
 * Incluye una etiqueta (label) y un valor (value).
 */
export interface TipoDeCertificoOption {
  label: string;
  value: string;
}
