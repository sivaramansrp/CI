import { Component } from '@angular/core';

/**
 * Componente Angular que representa el tercer paso en el flujo de trámites.
 * 
 * Este componente está diseñado para manejar la lógica y la presentación 
 * asociada al tercer paso de un proceso específico dentro de la aplicación.
 * 
 * @selector `app-paso-tres` - Selector utilizado para referenciar este componente en el HTML.
 * @templateUrl `./paso-tres.component.html` - Ruta del archivo HTML que define la estructura visual del componente.
 * @styleUrl `./paso-tres.component.scss` - Ruta del archivo SCSS que contiene los estilos específicos del componente.
 * 
 * @class PasoTresComponent
 * @description Clase que encapsula la lógica del tercer paso del trámite.
 */
@Component({
  selector: 'app-paso-tres',
  templateUrl: './paso-tres.component.html',
  styleUrl: './paso-tres.component.scss',
})
export class PasoTresComponent {}
