import { Component } from '@angular/core';

/**
 * Componente Angular que representa el paso de firmar la solicitud dentro del flujo de trámites.
 * 
 * Este componente se utiliza para mostrar la interfaz de usuario correspondiente al paso de firma
 * de la solicitud en el proceso de trámites. La plantilla asociada define la estructura visual y 
 * los elementos interactivos necesarios para completar este paso.
 * 
 * @selector app-paso-firmar-solicitud - Selector utilizado para referenciar este componente en el HTML.
 * @templateUrl ./paso-firmar-solicitud.component.html - Ruta al archivo de plantilla HTML que define la vista del componente.
 */
@Component({
  selector: 'app-paso-firmar-solicitud',
  templateUrl: './paso-firmar-solicitud.component.html',
})
export class PasoFirmarSolicitudComponent {
}
