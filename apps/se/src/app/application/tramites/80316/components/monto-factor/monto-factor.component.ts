import { Component } from '@angular/core';

/**
 * Componente `MontoFactorComponent` utilizado para gestionar y mostrar la información relacionada con el monto y el factor.
 * Este componente es independiente (standalone) y puede ser utilizado en diferentes partes de la aplicación.
 */
@Component({
  selector: 'app-monto-factor',
  templateUrl: './monto-factor.component.html',
  styleUrl: './monto-factor.component.scss',
  standalone: true
})
export class MontoFactorComponent {
  // Actualmente, este componente no contiene propiedades, métodos o lógica adicional.
  // Se puede extender en el futuro para incluir funcionalidad específica relacionada con el monto y el factor.
}
