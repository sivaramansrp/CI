
/**
 * Representa información de metadatos para una entidad CAAT Naviero (agente naviero).
 *
 * @interface CaatNaviroMetaInfo
 * @property {string} tutilo - El título o nombre asociado con el CAAT Naviero.
 * @property {string} tipoAgenteLabel - La etiqueta que describe el tipo de agente.
 */
export interface CaatNaviroMetaInfo {
  tutilo: string;
  tipoAgenteLabel: string;
}
