

import { Component } from '@angular/core';

/**
 * Componente: SolicitudTabComponentsComponent
 * 
 * Este componente representa una pestaña dentro de la solicitud de trámites.
 * Es responsable de gestionar y mostrar la interfaz de usuario asociada a esta sección específica.
 * 
 * Propiedades:
 * - `selector`: Define el nombre del selector que se utiliza para incluir este componente en una plantilla.
 * - `standalone`: Indica si el componente es independiente o no.
 * - `templateUrl`: Especifica la ubicación del archivo HTML asociado al componente.
 * - `styleUrl`: Especifica la ubicación del archivo SCSS asociado al componente.
 * 
 * Uso:
 * Este componente se utiliza como parte de la funcionalidad de solicitudes de trámites en la aplicación.
 */
@Component({
  selector: 'app-solicitud-tab-components',
  templateUrl: './solicitud-tab-components.component.html',
  styleUrl: './solicitud-tab-components.component.scss',
})
export class SolicitudTabComponentsComponent {}
