/**
 * HTML que representa un contenedor de errores con una lista de mensajes de error específicos.
 * 
 * Actualmente, muestra un solo error relacionado con el campo "Pago".
 * 
 */
export const SIGUIENTES_ERRORES = '<div class=\'error-container\'><p class=\'error-title\'>Corrija los siguientes errores:</p><p class=\'error-item\'><span class=\'error-index\'>1.</span><span class=\'error-text\'>(Pago) es un campo requerido</span></p></div>';

/**
 * Mensaje de error genérico utilizado cuando faltan campos por capturar en el formulario.
 * 
 */
export const ERROR_DE_CAMPO = 'Faltan campos por capturar';

/**
 * Mensaje de error general cuando el formulario contiene errores y necesita ser verificado.
 * 
 */
export const ERROR_VERIFICAR = 'Error en el formulario, favor de verificar';
