/**
 * Constante que representa la configuración por defecto para la fecha de pago.
 *
 * - `labelNombre`: Etiqueta visible para el campo de vigencia.
 * - `required`: Indica si el campo es obligatorio.
 * - `habilitado`: Determina si el campo está habilitado para su edición o interacción.
 */
export const PERFILES_FECHA_DE_PAGO = {
  /**
   * Nombre de la etiqueta que se mostrará en el campo de entrada.
   */
  labelNombre: 'Vigencia',

  /**
   * Indica si el campo es obligatorio.
   */
  required: false,

  /**
   * Determina si el campo está habilitado para la edición.
   */
  habilitado: true,
};

/**
 * Configuración del campo de entrada para perfiles con fecha.
 *
 * Define las propiedades básicas como el nombre de la etiqueta,
 * si es obligatorio y si se encuentra habilitado.
 */
export const PERFILES_FECHA_INPUT = {
  /**
   * Nombre de la etiqueta que se mostrará en el campo de entrada.
   */
  labelNombre: '',

  /**
   * Indica si el campo es obligatorio.
   */
  required: false,

  /**
   * Determina si el campo está habilitado para la edición.
   */
  habilitado: true,
};
