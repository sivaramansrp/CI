/**
 * Constante que representa la configuración por defecto para la fecha de pago.
 * 
 * - `labelNombre`: Etiqueta visible para el campo de vigencia.
 * - `required`: Indica si el campo es obligatorio.
 * - `habilitado`: Determina si el campo está habilitado para su edición o interacción.
 */
export const FECHA_DE_PAGO = {
  labelNombre: 'Vigencia',
  required: true,
  habilitado: true,
};
