user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log debug; # Incrementado nivel de log para debugging
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    
    # Formato de log detallado para debugging
    log_format debug_format '$remote_addr - $remote_user [$time_local] '
                           '"$request" $status $body_bytes_sent '
                           '"$http_referer" "$http_user_agent" '
                           '$request_time "$http_x_forwarded_for" '
                           '$host $request_filename';
    
    access_log /var/log/nginx/access.log debug_format;
    sendfile on;
    keepalive_timeout 65;
    client_max_body_size 100M; # Aumentado para subidas grandes
    
    # Habilitar compresión
    gzip on;
    gzip_comp_level 5;
    gzip_min_length 256;
    gzip_proxied any;
    gzip_vary on;
    gzip_types
      application/javascript
      application/json
      application/x-javascript
      text/css
      text/javascript
      text/plain
      text/xml;
    
    server {
        listen 80 default_server;
        server_name _;
        root /usr/share/nginx/html;
        index index.html;
        
        # CORS headers - importante para Module Federation
        add_header 'Access-Control-Allow-Origin' '*' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE, PATCH' always;
        add_header 'Access-Control-Allow-Headers' 'DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization' always;
        add_header 'Access-Control-Allow-Credentials' 'true' always;
        
        # Health check para Docker
        location ~ ^/health(\.txt)?$ {
            access_log off;
            add_header Content-Type text/plain;
            return 200 'OK';
        }
        
        # Ruta especial para el archivo remoteAppEntry.js - crítico para Module Federation
        location = /remoteAppEntry.js {
            add_header Cache-Control "no-store, no-cache, must-revalidate";
            add_header Pragma "no-cache";
            expires off;
            access_log on;
            try_files $uri =404;
        }
        
        # Assets críticos de Angular
        location ~* \.(js|mjs)$ {
            add_header Cache-Control "no-cache";
            expires -1;
            try_files $uri =404;
        }
        
        # Assets estáticos que se pueden cachear
        location ~* \.(css|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 7d;
            add_header Cache-Control "public, max-age=604800";
            access_log off;
            try_files $uri =404;
        }
        
        # Ruta principal - todas las solicitudes no manejadas se redirigen a index.html
        location / {
            try_files $uri $uri/ /index.html;
            add_header Cache-Control "no-store, no-cache, must-revalidate";
            add_header Pragma "no-cache";
            add_header X-Content-Type-Options "nosniff";
            expires -1;
        }
        
        # Configuración OPTIONS para CORS
        location = /options-preflight {
            if ($request_method = 'OPTIONS') {
                add_header 'Access-Control-Allow-Origin' '*';
                add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE';
                add_header 'Access-Control-Allow-Headers' 'DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization';
                add_header 'Access-Control-Max-Age' 1728000;
                add_header 'Content-Type' 'text/plain charset=UTF-8';
                add_header 'Content-Length' 0;
                return 204;
            }
            return 301 /;
        }
        
        # Denegar archivos ocultos
        location ~ /\. {
            deny all;
            access_log off;
            log_not_found off;
        }
        
        # Página de error personalizada
        error_page 404 /index.html;
    }
}