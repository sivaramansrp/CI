import { Component } from '@angular/core';

/**
 * 
 * @selector app-paso-firmar-solicitud
 * @template ./paso-firmar-solicitud.component.html
 * 
 * @description
 * Este componente encapsula la lógica y la presentación necesarias para permitir que el usuario
 * firme una solicitud como parte del flujo de trámites. Proporciona una interfaz interactiva
 * para realizar la firma digital o manual según sea necesario.
 */
@Component({
  selector: 'app-paso-firmar-solicitud',
  templateUrl: './paso-firmar-solicitud.component.html',
})

/**
 * Componente que representa el paso de firmar la solicitud en el flujo de trámites.
 * Este componente se utiliza para gestionar la firma de la solicitud por parte del usuario.
 * 
 * @remarks
 * Este componente es parte del módulo de trámites y se encuentra en la página correspondiente
 * al paso de firma de la solicitud.
 * 
 * @author Muneeshwaran N
 * @file apps/semarnat/src/app/application/tramites/230401/pages/paso-firmar-solicitud/paso-firmar-solicitud.component.ts
 */
export class PasoFirmarSolicitudComponent {
}
