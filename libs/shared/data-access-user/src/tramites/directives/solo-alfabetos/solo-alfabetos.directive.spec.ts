import { SoloAlfabetosDirective } from './solo-alfabetos.directive';

describe('SoloAlfabetosDirective', () => {
  it('should create an instance', () => {
    const directive = new SoloAlfabetosDirective();
    expect(directive).toBeTruthy();
  });
});
