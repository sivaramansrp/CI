import { HoraFormatoDirective } from './hora-formato.directive';

describe('HoraFormatoDirective', () => {
  it('should create an instance', () => {
    const directive = new HoraFormatoDirective();
    expect(directive).toBeTruthy();
  });
});
