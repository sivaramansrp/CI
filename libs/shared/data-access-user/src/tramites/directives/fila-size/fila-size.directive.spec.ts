import { FilaSizeDirective } from './fila-size.directive';

describe('FilaSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new FilaSizeDirective();
    expect(directive).toBeTruthy();
  });
});
