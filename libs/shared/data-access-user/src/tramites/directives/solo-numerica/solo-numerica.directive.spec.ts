import { SoloNumericaDirective } from './solo-numerica.directive';

describe('SoloNumericaDirective', () => {
  it('should create an instance', () => {
    const directive = new SoloNumericaDirective();
    expect(directive).toBeTruthy();
  });
});
