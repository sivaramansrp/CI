import { RestringirNegativosDirective } from './restringir-negativos.directive';

describe('RestringirNegativosDirective', () => {
  it('should create an instance', () => {
    const directive = new RestringirNegativosDirective();
    expect(directive).toBeTruthy();
  });
});
