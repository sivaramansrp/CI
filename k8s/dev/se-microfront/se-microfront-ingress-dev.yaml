# Create Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: modulefederation-se-microfront-ingress-dev
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
  - host: front.v30.ultrasist.net
    http:
      paths:
      - path: /se(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: modulefederation-se-microfront-service-dev
            port:
              number: 4205
  tls:
  - hosts:
    - front.v30.ultrasist.net
    secretName: v30certs

