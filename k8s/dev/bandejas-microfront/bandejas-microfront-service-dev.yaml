# Expose the deployment using a Service
apiVersion: v1
kind: Service
metadata:
  name: modulefederation-bandejas-microfront-service-dev
spec:
  selector:
    app: modulefederation-bandejas-microfront-dev
  ports:
    - protocol: TCP
      port: 4224
      targetPort: 80
  type: NodePort
