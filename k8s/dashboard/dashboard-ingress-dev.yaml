# Create Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: modulefederation-dashboard-ingress-dev
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rules:
  - host: front.v30.ultrasist.net
    http:
      paths:
      - path: /dashboard
        pathType: Prefix
        backend:
          service:
            name: modulefederation-dashboard-service-dev
            port:
              number: 4200
  tls:
  - hosts:
    - front.v30.ultrasist.net
    secretName: v30certs
