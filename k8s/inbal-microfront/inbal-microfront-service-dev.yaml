# Expose the deployment using a Service
apiVersion: v1
kind: Service
metadata:
  name: modulefederation-inbal-microfront-service-dev
spec:
  selector:
    app: modulefederation-inbal-microfront-dev
  ports:
    - protocol: TCP
      port: 4218
      targetPort: 80
  type: NodePort
