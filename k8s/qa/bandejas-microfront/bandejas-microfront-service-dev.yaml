# Expose the deployment using a Service
apiVersion: v1
kind: Service
metadata:
  name: modulefederation-bandejas-microfront-service-qa
spec:
  selector:
    app: modulefederation-bandejas-microfront-qa
  ports:
    - protocol: TCP
      port: 4224
      targetPort: 80
  type: NodePort
