# Expose the deployment using a Service
apiVersion: v1
kind: Service
metadata:
  name: modulefederation-privados-microfront-service-qa
spec:
  selector:
    app: modulefederation-privados-microfront-qa
  ports:
    - protocol: TCP
      port: 4210
      targetPort: 80
  type: NodePort
