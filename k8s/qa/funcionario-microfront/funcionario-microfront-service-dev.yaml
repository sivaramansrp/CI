# Expose the deployment using a Service
apiVersion: v1
kind: Service
metadata:
  name: modulefederation-funcionario-microfront-service-qa
spec:
  selector:
    app: modulefederation-funcionario-microfront-qa
  ports:
    - protocol: TCP
      port: 4210
      targetPort: 80
  type: NodePort
