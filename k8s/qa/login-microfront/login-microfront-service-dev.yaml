# Expose the deployment using a Service
apiVersion: v1
kind: Service
metadata:
  name: modulefederation-login-microfront-service-qa
spec:
  selector:
    app: modulefederation-login-microfront-qa
  ports:
    - protocol: TCP
      port: 4201
      targetPort: 80
  type: NodePort
