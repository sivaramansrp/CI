# Expose the deployment using a Service
apiVersion: v1
kind: Service
metadata:
  name: modulefederation-amecafe-microfront-service-qa
spec:
  selector:
    app: modulefederation-amecafe-microfront-qa
  ports:
    - protocol: TCP
      port: 4212
      targetPort: 80
  type: NodePort
