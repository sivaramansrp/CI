# Expose the deployment using a Service
apiVersion: v1
kind: Service
metadata:
  name: modulefederation-dashboard-service-dev
spec:
  selector:
    app: modulefederation-dashboard-dev
  ports:
    - protocol: TCP
      port: 4200
      targetPort: 80
  type: NodePort
