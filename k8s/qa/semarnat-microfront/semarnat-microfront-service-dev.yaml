# Expose the deployment using a Service
apiVersion: v1
kind: Service
metadata:
  name: modulefederation-semarnat-microfront-service-qa
spec:
  selector:
    app: modulefederation-semarnat-microfront-qa
  ports:
    - protocol: TCP
      port: 4206
      targetPort: 80
  type: NodePort
