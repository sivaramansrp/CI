# Expose the deployment using a Service
apiVersion: v1
kind: Service
metadata:
  name: modulefederation-se-microfront-service-qa
spec:
  selector:
    app: modulefederation-se-microfront-qa
  ports:
    - protocol: TCP
      port: 4205
      targetPort: 80
  type: NodePort
