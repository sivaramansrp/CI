import { CalendarioFormatoDirective } from './calendario-formato.directive';

describe('CalendarioFormatoDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarioFormatoDirective();
    expect(directive).toBeTruthy();
  });
});
