version: '3'

services:
  dashboard:
    build:
      context: .
      dockerfile: ./apps/dashboard/Dockerfile
    image: local/dashboard:test
    ports:
      - "8080:80"
    container_name: test-dashboard