services:
#---------------------------------------------------------------
  modulefederation-agricultura-microfront:
    image: ghcr.io/vucem30/dockerized-modulefederation-agricultura-microfront:1.0.15
    build: 
      context: ./../
      dockerfile: apps/agricultura/Dockerfile.dev
      args:
        - NODE_ENV=production
    container_name: modulefederation-agricultura-microfront
    restart: unless-stopped
    expose:
      - '4204'
    ports:
      - "4204:80"
    networks:
      - modulefederation-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    environment:
      - NODE_ENV=production
    volumes:
      - nginx_logs:/var/log/nginx
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
#----------------------------------------------------------------
networks:
  modulefederation-network:
    driver: bridge

volumes:
  nginx_logs:
