services:
#----------------------------------------------------------------
  modulefederation-dashboard:
    image: modulefederation-dashboard:1.0.0
    build: 
      context: ./../
      dockerfile: apps/dashboard/Dockerfile
    container_name: modulefederation-dashboard
    restart: on-failure
    expose:
      - '4200'
    ports:
      - "4200:80"
    networks:
      - modulefederation-network
#----------------------------------------------------------------
networks:
  modulefederation-network:
